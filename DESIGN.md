# Codex Quota 设计约束

## 产品形态

- 这是常驻菜单栏的单用途工具，不创建主窗口，不显示 Dock 图标。
- 菜单栏只表达最重要的信息：仪表图标、当前模型来源，以及 Codex 最低剩余百分比、DeepSeek 剩余金额或当前 GLM 型号；存在需要关注的任务时，右侧追加语义彩色摘要：待审核为橙色、未查看完成为绿色、运行中为蓝色。多任务只显示最高优先级状态及数量，优先级固定为“待审核 → 未查看完成 → 运行中”。
- 弹窗固定宽 360pt，高度优先跟随内容自然伸展，并按当前屏幕可用区域自动封顶；只有内容确实超过屏幕安全高度时才滚动内容区，操作区始终保持可见。
- 主看板在来源切换与额度区域之间展示“任务动态”，只包含待审核、运行中和完成但未查看的任务；空分组不占位，全部为空时显示紧凑完成态。

## 视觉语言

- 固定使用参考图风格的暖灰浅色主题。
- Base `#F2F2F0`，Mantle `#FAFAF8`，Surface `#E8E8EA`，Panel `#FFFFFF`，Text `#18181A`。
- 主强调色使用 Blue `#2F7DF6`，类型辅助色使用 Purple `#7C3AED`；任务完成使用 Green `#34A853`，警告和紧张额度分别使用 Orange `#D97706`、Rose `#E11D48`。
- 使用平面分区与 1pt 分隔线，不把每项信息包装成独立卡片；额度信息集中在一个紧凑的液态玻璃聚合面板内，通过色点、蓝 / 橙 / 玫红渐变分段轨道和数字建立横向映射。
- 文字使用系统字体，数字使用等宽数字；图标仅使用 SF Symbols。
- 正式菜单栏界面与预览入口统一使用无边框透明 `NSPanel` 承载，不叠加系统 `NSPopover` 自带的材质底板；面板以状态栏按钮为锚点定位。最外层窗口级 `NSVisualEffectView` 使用完整强度的 `Popover` 材质并叠加约 94% 暖灰实体底，形成稳定的厚磨砂底板，只让极少量环境色参与。中间额度看板与底部功能看板复用完整强度的独立 WithinWindow 材质，并叠加约 95.5% 暖白填充；层级主要通过细暗边、顶部柔和高光和低对比阴影表达，不依赖透出桌面背景。内容层保持 100% 不透明，优先保障文字、进度条和操作控件的对比度。开启“减少透明度”时各材质层自动回退为近实色暖灰；开启“增强对比度”时同步强化实体填充与边界。
- 悬浮操作层使用高不透明暖白、细暗边和低对比高光表达轻质材质，不叠加多层玻璃。

## 交互与动效

- 面板打开时立即刷新；后台以事件更新为主、30 秒轮询为辅。
- 本机任务状态每秒只读校准；菜单栏状态摘要按“`!数量` / `✓数量` / `●数量`”三选一显示，悬浮说明提供三组完整数量，不运行持续动画。
- 任务行必须显示标题、项目、状态文字和开始 / 完成时间，不使用无法验证的进度条。新出现的任务行使用 160ms 克制进入反馈；开启“减少动态效果”时只保留淡入。
- 打开面板不会自动清除完成提醒。Codex 切到前台时，所有已完成任务自动标记为已查看；若任务在 Codex 已位于前台时完成，也直接视为已查看。点击已完成任务或“全部已查看”仍可手动清除，状态跨启动保存，最多保留 7 天。
- “待审核”只允许由 Codex 明确的运行时信号驱动；本机持久化数据无法区分审核等待与普通执行时，统一安全回退为“运行中”。
- 菜单栏面板始终锚定状态栏按钮并限制在当前屏幕可见区域内；再次点击状态栏按钮、按下 Escape 或面板失去焦点时关闭，页面切换与高度变化后重新对齐同一锚点。
- 额度分段轨道复用现有视图并从旧值平滑过渡，使用 240ms 强 ease-in-out；已填充分段带有克制的同色柔光，以强化额度状态映射但不形成持续动画；普通刷新不对整个额度区做闪烁式淡入。
- 额度行使用紧凑密度，长模型名允许截断但必须优先保留右侧百分比；底部低频辅助操作使用无框工具栏按钮，不与主操作争夺层级。
- 次级功能按钮在指针悬停时浮出玻璃胶囊并改变前景色，离开后快速收回；主额度页使用紧凑的“刷新”按钮，保持主操作辨识度但不压过额度信息。
- 鼠标按压按钮时提供 120ms、`scale(0.97)` 的即时反馈；新额度行首次出现时以 40ms 间隔做不阻塞交互的短暂错峰进入。
- 开启“减少动态效果”时禁用位移和缩放，只保留短透明度反馈；不运行持续装饰动画。
- 所有状态必须提供文字，不只依赖颜色；菜单栏图标保持模板单色。
- 底部操作区始终包含刷新、登录（需要时）、选择 Codex、退出和登录项开关。
- 面板顶部提供 Codex 订阅 / DeepSeek / GLM 分段开关；外部模型模式显示具体模型和更换 API Key 操作，DeepSeek 额外显示余额，GLM 显示 Responses 接入协议。
- 额度页右上角提供“扩展”入口；扩展管理仍使用 360pt 宽弹窗，以全部 / Skill / 插件分段筛选，列表区滚动、底部检查操作固定。
- 扩展条目必须同时显示类型、来源、作用域、用途说明和文字状态；用途说明单行截断，并在鼠标悬浮时通过自动换行的系统提示展示全文。
- 额度页与扩展管理页切换时始终保持 360pt 容器宽度，只允许高度按页面内容调整。
- 弹窗失去焦点并重新打开时保持当前页面；只有用户点击“返回”或“扩展”入口时才切换页面。
- 每个 Skill 提供紧凑的文件夹图标按钮及完整悬浮说明；单安装位置直接打开目录，多安装位置或飞书套件在 Finder 中集中显示全部目录。
- 每个 Skill 提供红色“卸载”操作；执行前必须明确提示删除范围与不可撤销性，执行期间禁用更新、检查和其他卸载操作，失败时给出可恢复的具体原因。
- 飞书官方 Skills 不逐项铺开，以单个“飞书 CLI Skills”套件展示 Skill 数量、覆盖能力和 CLI 版本；只有发现更新时启用“更新”，本地维护、检查中、已最新和失败状态均不可伪装成可操作状态。
- 扩展页底部明确提示每 6 小时自动检查，并保留手动“检查更新”操作；更新进度与失败原因就地显示。
- API Key 安全输入框支持标准 `⌘V` 粘贴快捷键；模型配置写入成功后询问是否立即重启 Codex。

## 数据与隐私

- Codex 额度模式只与本机 `codex app-server --stdio` 通信；任务动态只读查询 `~/.codex` 下的本机会话索引与 turn 状态数据库，不修改数据库或读取消息正文；DeepSeek 模式只请求官方 `/user/balance`；GLM 模式只管理指向智谱 `/api/v1` Responses 端点的 Codex 配置。
- 从应用重启 Codex 时注入本地历史兼容桥，只改写 `thread/list` 的 provider 过滤条件，不记录或改写任何对话内容与会话数据库。
- 不读取 `auth.json`，不持久化 Codex 登录令牌，不记录登录 URL 或完整服务响应。
- DeepSeek 与 GLM API Key 按各自接入要求写入 Codex 配置，并使用独立服务项额外保存到 macOS 钥匙串；不写日志、不在界面回显。
- 工作区额度仅在服务端明确返回 credits 字段时显示。
